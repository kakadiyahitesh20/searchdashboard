<!DOCTYPE html>
<html lang="en">
<head>
    <% include head %>
</head>a
<body  ng-app="myapp">
<% include header %>
<div class="container-fluid">
    <section class="container">
        <div class="container-page">
            <div class="col-md-6">
                <form name="fpass">
                    <h3 class="dark-grey">Forgot Password</h3>
                    <div class="form-group col-lg-12">
                        <label>Enter your existing Email</label>
                        <input type="email" name="email" class="form-control" id="femail" value="" ng-model="femail" required>
                    </div>
                    <div class="form-group col-lg-12">
                        <input type="submit" name="" class="btn btn-danger" id="submit" value="Submit" ng-disabled="fpass.$invalid">
                    </div>
                </form>
                <div id="fmessage"></div>
            </div>

        </div>
    </section>
</div>
<% include footer %>
</body>

</html>
<script>
    $(document).ready(function(){
        var email,pass;
        $("#submit").click(function(){
            email=$("#femail").val();
            /*
             * Perform some validation here.
             */
                $.post("/fpassword",{email:email},function(data){
                    if(data==='done')
                    {
                        $("#fmessage").append("<p>We have sent email with link</p>");
                    }
                    else {
                        $("#fmessage").append("<p>Email does't exit</p>");
                    }
                });

        });
    });
</script>
